# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.venv/
*.egg-info/

# Node
node_modules/

# Environment & secrets
.env
*.env
ai_studio_config.json
backend/ai_studio_config.json

# Claude local config
.claude/

# Chatroom workflow trace artifacts (debug output, not source)
chatroom/data/workflows/traces/*.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# ChatRoom
chatroom/node_modules/
chatroom/client/node_modules/
chatroom/.env

# Output
Synthograsizer_Output/

# Misc
.pytest_cache/
.tox/
dist/
*.log

# Dev References
scope-synthograsizer/

# Internal planning & debug (not for public repo)
.internal/

# Personal / private templates (not for public repo)
static/synthograsizer/templates/_private/

# Archive / secondary surfaces (preserved on disk, not tracked)
static/legacy/
static/promptcraft/
static/daw/
static/fun-stuff/
static/metadata-manager/

# Capacitr storyboard run artifacts (regenerable; archived under Synthograsizer_Output/JSON/Capacitr Workflows/)
data/capacitr/
static/synthograsizer/capacitr-gallery*/

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# The durable inputs under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, guidelines are in design-system/) ARE committed; only these are not.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
# Self-hosted webfonts are copied from the @fontsource dev deps at build time.
design-system/fonts/files/
