---
category: Controls
---

Inline checkbox with a label and an optional muted hint — the hint carries cost or behavior consequences so the choice is informed at a glance.

```jsx
<CheckField
  label="Consistent characters"
  hint="character bible + keyframes (adds ~20–35% cost)"
/>
<CheckField label="Full auto" hint="skip approval checkpoints" defaultChecked />
```
