import { ActionRow, Button } from '@synthograsizer/design-system';

export const InspectorActions = () => (
  <ActionRow>
    <Button>Save</Button>
    <Button variant="primary">Save &amp; Retake (~$0.35–0.70)</Button>
    <Button>Suggest variations</Button>
  </ActionRow>
);
