import { Badge } from '@synthograsizer/design-system';

export const Flags = () => (
  <div style={{ display: 'flex', gap: 8, flexWrap: 'wrap' }}>
    <Badge>likeness flag</Badge>
    <Badge>content filter</Badge>
  </div>
);
